export { default as chatPage } from './chatPage.hbs?raw';

import { setupFormValidation } from '../../utils/validation';

export function initChatPage(): void {
  // Logic moved to chatPage.ts component
}

<main>
  <div class='change-password-page'>
    <div class='change-password-card'>
      <div class='change-password-avatar'></div>

      <form class='change-password-form'>
        <div class='form-row'>
          <label for='old-password' class='form-label'>Старый пароль</label>
          {{> Input id="old-password" name="oldPassword" type="password" }}
        </div>
        <div class='form-row'>
          <label for='new-password' class='form-label'>Новый пароль</label>
          {{> Input id="new-password" name="newPassword" type="password" }}
        </div>
        <div class='form-row'>
          <label for='repeat-password' class='form-label'>Повторите новый пароль</label>
          {{> Input id="repeat-password" name="repeatPassword" type="password" }}
        </div>

        <div class='change-password-actions'>
          {{> Button type="submit" text="Сохранить" }}
        </div>
      </form>
    </div>
  </div>
</main>
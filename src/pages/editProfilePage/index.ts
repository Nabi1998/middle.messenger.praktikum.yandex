export { default as editProfilePage } from './editProfilePage.hbs?raw';

import { setupFormValidation } from '../../utils/validation';

export function initEditProfilePage(): void {
  // Logic moved to editProfilePage.ts component
}
